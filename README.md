# Nautobot Ecosystem Health

## Getting started

- `./run.sh` setup (installs deps via poetry)
- `./run.sh` build (builds html pages in the `output` folder)
- `./run.sh` serve (builds pages and starts a local web server)
